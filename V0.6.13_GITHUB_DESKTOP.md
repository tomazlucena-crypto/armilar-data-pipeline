# Publicar a v0.6.13 com GitHub Desktop

## Branch

Usa a branch existente:

`codex/step2h0-remaining-country-audits`

O pull request existente será actualizado automaticamente quando fizeres push. Não abras outro PR.

## Substituir os ficheiros

1. No GitHub Desktop, selecciona `codex/step2h0-remaining-country-audits`.
2. Faz `Fetch origin` e `Pull origin`.
3. Escolhe `Repository` > `Show in Explorer`.
4. Extrai `armilar-data-pipeline-v0613.zip` para uma pasta temporária.
5. Na pasta local do repositório, apaga tudo excepto a pasta oculta `.git`.
6. Copia para a pasta local todo o conteúdo que está dentro de `armilar-data-pipeline-v0613`.
7. Volta ao GitHub Desktop e confirma que as alterações aparecem na branch correcta.

## Testar antes do commit

Em `Repository` > `Open in Terminal`:

```powershell
py -m pip install -e .
py -m pytest -q
py -m unittest discover -s tests -v
```

Resultados esperados:

- pytest: `110 passed, 5 subtests passed`
- unittest: `Ran 110 tests` e `OK`

## Commit e push

Resumo do commit:

`Complete second-wave and Step 2H source audits`

Depois:

1. `Commit to codex/step2h0-remaining-country-audits`
2. `Push origin`
3. Abre o PR existente e acompanha o novo workflow.

Não faças merge até todos os checks ficarem verdes.

## Conteúdo acumulado

- correcção da linha malformada do Brasil que fez falhar o workflow;
- validação mais forte do esquema CSV;
- auditorias da Indonésia e Brasil preservadas;
- auditorias do Egipto, Paquistão, Nigéria, Bangladesh e Vietname;
- auditorias executáveis das excepções Bielorrússia CP02, Kuwait CP02, Arábia Saudita CP02, Bonaire e Libéria;
- 65 recursos oficiais ou famílias de fontes em 15 economias;
- zero células exactas acrescentadas;
- `weights_final.csv` continua vazio;
- política monetária continua bloqueada.
