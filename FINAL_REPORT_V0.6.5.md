# Final report v0.6.5

The China source audit is implemented as a deterministic, fail-closed adapter. Six official NBS resources are attempted independently and preserved when acquired. The adapter rejects the eight-group survey as Class C, records the 2020 product-based input-output mismatch and keeps the 2021 GDP verification as aggregate evidence only.

No exact cells were added. `weights_final.csv` remains empty, `global_12_category_matrix_complete=false`, `monetary_release_allowed=false`, and Step 2J remains unstarted.

This version is staging-only and contains no GitHub push or pull-request instructions.
