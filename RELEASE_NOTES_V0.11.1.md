# ARMILAR v0.11.1

Defines the controlled external ECOICOP v1/v2 dual-panel intake runner.

This milestone can consume a verified v0.10.7 attachment descriptor and the
materialized external panel artifact behind it, then produce a deterministic
intake report for a future empirical transition backtest. It does not acquire
live provider bytes inside the PR, commit official bytes, modify `public/latest`,
run an empirical backtest, select T0/T1/T2/T3, or open any research or monetary
gate.

Status emitted by the checker:

```text
ECOICOP_V2_TRANSITION_BACKTEST_EXTERNAL_PANEL_INTAKE_V0111_VALID
```
