# v0.6.13 staging notes

Target branch: `codex/step2h0-remaining-country-audits`

Suggested commit: `Complete second-wave and Step 2H source audits`

Before pushing, run:

```powershell
py -m pip install -e .
py -m pytest -q
py -m unittest discover -s tests -v
```

Expected result: `110 passed` plus five passing subtests in pytest, and `110 tests` in unittest.

Do not merge while GitHub Actions is red.
