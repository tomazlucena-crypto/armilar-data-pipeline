# v0.6.5 staging notes

Do not publish this version separately. Preserve it as the cumulative local base for the next country audit.

Validation commands:

```bash
python -m pip install -e .
python -m unittest discover -s tests -v
python -m pytest -q
armilar-country acquire CHN --config config/step2_icp2021.json --run-dir run-china --cache-dir .cache/armilar
```

No branch, push or pull request should be created until all priority economies are complete.
