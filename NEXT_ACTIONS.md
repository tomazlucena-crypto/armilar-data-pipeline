# Next actions after v0.9.4

The immediate objective is to declare the Research Core scope and vertical roadmap before any executable expansion.

1. conclude the v0.9.5 scope documentation;
2. ratify constitution and contracts;
3. build the official engine;
4. introduce temporal storage;
5. integrate proxies;
6. build the live estimator;
7. add reconciliation and state handling;
8. operationalise the pipeline;
9. create the internal API;
10. create the shadow dashboard;
11. begin shadow production.

## Do not start yet

- expansion to new countries;
- advanced model optimisation;
- MLflow;
- Prefect or Dagster;
- public API work;
- blockchain;
- monetary policy claims;
- basket weight changes;
- live acquisition in CI.

## Current state

- baseline: v0.9.4;
- PR #16 is integrated in `main`;
- v0.9.4 validated official acquisition, replay, pre-release backtests and paired diagnostics;
- `public/latest` remains unchanged;
- `research_release_allowed=false`;
- `model_promotion_allowed=false`;
- `monetary_release_allowed=false`.
